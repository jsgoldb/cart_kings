<!--   <div class="container">
    <ul class="row list-unstyled">
      <% @carts.each do |cart| %>
        <li class="col-xs-4 <%= 'rotate' unless dont_rotate(cart) %>">
          <a href="#" class="thumbnail">
            <img src=<%= cart.image.url %> alt="">
          </a>
        </li>
      <% end %>
    </ul>
  </div>  -->

    <% @carts.each do |cart| %>

    <div class="col-xs-6 col-sm-3 thumbnail cart ">

      <div class="row">
        <%= link_to "View Cart to Edit/Delete", cart_path(cart) %>
      </div>

      <div class="cart_comments row">
        <%= cart.comments %>
      </div>
      

        <img src="<%= cart.image.url %>" alt="<%= cart.comments %>" class="<%= 'rotate' unless dont_rotate(cart) %>"/>


      <%= button_to 'View This Cart', carts_path, {class: 'btn btn-primary'}  %>
    </div>

  <% end %>
 